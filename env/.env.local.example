# Copy this file to ../.env.local and fill in credentials for local development
# Do NOT commit .env.local to source control (it's ignored by .gitignore)

# Primary DB (used as default DB)
DB_HOST=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=YourStrong!Passw0rd
DB_DATABASE=GHC_SBO

# Optional Sensus-specific overrides (if you run sensus on a separate DB)
# Either set SENSUS_CONN (full connection string) or the DB_SENSUS_* variables
# Example connection string format for SQL Server:
# Server=localhost,1433;Database=GHC_SBO;User Id=sa;Password=YourStrong!Passw0rd;Encrypt=false
SENSUS_CONN=
SENSUS_DATABASE=

DB_SENSUS_HOST=
DB_SENSUS_PORT=1433
DB_SENSUS_USER=
DB_SENSUS_PASSWORD=
DB_SENSUS_DATABASE=

# Optionally define a specific stored procedure name to call
SENSUS_PROC=dbo.CurrentSensus

# Fallback or secondary DB (optional)
DB2_HOST=
DB2_PORT=1433
DB2_USER=
DB2_PASSWORD=
DB2_DATABASE=GHS_FwApps

# Any other env vars used by the app
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Notes:
# - Copy this file to the project root as `.env.local` and populate secrets.
# - For local SQL Server, set `Encrypt=false` in connection strings if needed.
# - The API at /api/sensus-data will look for `SENSUS_PROC`, `SENSUS_DATABASE`,
#   `DB_SENSUS_*` or fall back to `DB_*` values above.
